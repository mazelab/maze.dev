#!/bin/bash
#
# maze nginx script head - templates/mail/nginx/script-head.erb
#
#
# this file was generated by puppet
# do not overwrite this file, or puppet will overwrite it again...
# so only customize this file in puppet itself
#
###############################################################################

# application settings
MAZEHOST="<%= @mazeHost %>"
NODEIP="<%= @nodeIp %>"

# maze user settings
HOMEDIR="<%= @homedir %>"

NGINX_DIR="<%= @config_dir %>"
NGINX_DIR_AVAILABLE="${NGINX_DIR}/sites-available"
NGINX_DIR_ENABLED="${NGINX_DIR}/sites-enabled"

NGINX_PROCESS="<%= @service %>"
NGINX_PROCESS_USER="<%= @process_user %>"

NGINX_COMMAND_STATUS="<%= @command_status %>"
NGINX_COMMAND_CONFIGTEST="<%= @command_configtest %>"
NGINX_COMMAND_RELOAD="<%= @command_reload %>"

